name: Deploy Next.js to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: './dcfs-gh-pages/package-lock.json'

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          static_site_generator: next

      - name: Restore cache
        uses: actions/cache@v5
        with:
          path: |
            ./dcfs-gh-pages/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('./dcfs-gh-pages/package-lock.json') }}-${{ hashFiles('./dcfs-gh-pages/**/*.js', './dcfs-gh-pages/**/*.jsx', './dcfs-gh-pages/**/*.ts', './dcfs-gh-pages/**/*.tsx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('./dcfs-gh-pages/package-lock.json') }}-

      - name: Install dependencies
        run: npm ci
        working-directory: ./dcfs-gh-pages

      - name: Build with Next.js
        run: npm run build
        working-directory: ./dcfs-gh-pages

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./dcfs-gh-pages/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
